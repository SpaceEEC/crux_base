use Mix.Config

# config :crux_base,
#    # required
#    token: "your token",

# You can configure all other applications like usual
# Note that url and shard_count will be fetched automatically if omitted.

# config :crux_gateway,
#    url: "wss://gateway.discord.gg",
#    shard_count: 5,
#    shards: [1, 2, 3..5]
#    dispatcher: GenStage.BroadcastDispatcher

# config :crux_cache,
#    guild: CustomCache.Guild,
#    channel: CustomCache.Channel,
#    emoji: CustomCache.Emoji,
#    presence: CustomCache.Presence,
#    user: CustomCache.User

# etc
